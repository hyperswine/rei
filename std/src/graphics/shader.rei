@!feature = Shader

// NOTE
// shader functions are basically regular functions, but get compiled to different instructions

// so uhh use the spectre isa? dont bother about it here? Well some systems dont have it
// so keep this for now?
// generate: () {}
